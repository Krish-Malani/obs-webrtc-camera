export const metadata = {
  title: "Receiver",
  description: "Connect your phone and receive the wireless webcam feed on your PC.",
};

export default function ReceiverLayout({ children }) {
  return children;
}
